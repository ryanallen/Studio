# Skills

Reusable workflows invoked by trigger phrase or `/skill-name`. Each skill lives in a kebab-case folder with `SKILL.md`. In Claude Code and Cursor, `/skills` lists all. The task checklist ([verify-task](verify-task/SKILL.md)) is run first every turn via `npm run checklist -- "<summary>"` (coordinator Step 1).
